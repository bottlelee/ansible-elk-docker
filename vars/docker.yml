---

docker:
  apt:
    key: http://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg
    repository: "deb [arch=amd64] http://mirrors.aliyun.com/docker-ce/linux/ubuntu {{ ansible_distribution_release }} stable"

  yum:
    repository: http://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo

  registry:
    mirrors:
      - https://registry.docker-cn.com
